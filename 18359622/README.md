# Zenodo Wake-Up Package (v1.1)

**Project**: Operational Consciousness as a Global Rescue Controller  
**DOI**: 10.5281/zenodo.18359622

## What this ZIP is
A Zenodo-ready, root-flat archive containing:
- the full audited white paper text (**WHITE_PAPER.md**),
- a scope manifest (**MANIFEST.yaml**),
- licensing (**LICENSE.txt**),
- integrity checksums (**CHECKSUMS_SHA256.txt**, **CHECKSUMS_SHA512.txt**),
- a transparency script (**run.sh**) that explicitly states this is a document-first reproducibility package.

## Reproducibility statement (strict)
This package is **documentary-reproducible**:
- every claim is tied to *public datasets* and a *falsification protocol* described in the paper,
- the intended replication is an **independent re-run** of the protocol against public data,
- no hidden code or private data are required to evaluate the logical structure.

If you publish an empirical refutation:
1) use public datasets of comparable type,  
2) apply the same negative controls and transfer tests,  
3) report PASS/FAIL for each hypothesis with evidence.

## Integrity
Verify file integrity using the provided checksums.

## Root-flat constraint
All files are intentionally stored at the ZIP root to match the requested Zenodo packaging style.